function renderQuickStats(
    season,
    container
) {

    container.innerHTML = `

        <!-- Paste existing Quick Stats HTML here -->

    `;

}

window.renderPlayerQuickStats =
    renderQuickStats;