// ======================================================
// PLAYER RENDERERS
// Classic Browser Script Registry
// ======================================================

window.PlayerRenderers = {

    renderHero:
        window.renderPlayerHero,

    renderQuickStats:
        window.renderPlayerQuickStats,

    renderSeasonSnapshot:
        window.renderPlayerSeasonSnapshot,

    renderTrendCards:
        window.renderPlayerTrendCards,

    renderTrendDetails:
        window.renderPlayerTrendDetails,

    renderGameLogs:
        window.renderPlayerGameLogs,

    renderSummary:
        window.renderPlayerSummary

};